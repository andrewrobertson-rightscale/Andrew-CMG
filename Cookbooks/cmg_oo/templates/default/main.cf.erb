###
# Generated by Chef for ip-10-0-0-124.ec2.internal
# Configured as master
###

biff = no
append_dot_mydomain = no
smtpd_use_tls = yes
smtpd_tls_cert_file=/etc/ssl/certs/ssl-cert-snakeoil.pem
smtpd_tls_key_file=/etc/ssl/private/ssl-cert-snakeoil.key
smtpd_tls_session_cache_database = btree:${data_directory}/smtpd_scache
smtp_tls_session_cache_database = btree:${data_directory}/smtp_scache
myhostname = <%= (@host != nil) ? @host.gsub(".", "\-") : "127-0-0-1" %>.ec2.internal
mydomain = ec2.internal
myorigin = $myhostname
smtpd_banner = $myhostname ESMTP $mail_name
alias_maps = hash:/etc/aliases
alias_database = hash:/etc/aliases
mydestination = <%= (@host != nil) ? @host.gsub(".", "\-") : "127-0-0-1" %>.ec2.internal, <%= (@host != nil) ? @host.gsub(".", "\-") : "127-0-0-1" %>, localhost.localdomain, localhost
mynetworks = 127.0.0.0/8
inet_interfaces = all
mailbox_size_limit = 0
recipient_delimiter = +

milter_default_action = tempfail
milter_protocol = 6
smtpd_milters =
non_smtpd_milters =

#sendgrid
#smtp_sasl_auth_enable = yes
#smtp_sasl_password_maps = static:cmg-gb-sendgrid_email:03ESdg0Qyb3qNg7R6HqW
#smtp_sasl_security_options = noanonymous
#smtp_tls_security_level = encrypt
#header_size_limit = 4096000
#relayhost = [smtp.sendgrid.net]:587
